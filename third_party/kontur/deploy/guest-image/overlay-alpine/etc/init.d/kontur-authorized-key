#!/sbin/openrc-run

description="Install the kontur exec SSH key from the kernel command line"

depend() {
	need localmount
	before sshd
}

start() {
	ebegin "Installing the kontur exec SSH key"
	/usr/local/libexec/kontur-authorized-key
	eend $?
}
