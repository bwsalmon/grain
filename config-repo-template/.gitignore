# Nothing here holds a credential. Secrets go in this repo's Actions
# secrets and, from there, straight into the host's own instance
# metadata. (Terraform itself -- state, its own .terraform/ cache -- lives
# in grain's terraform/gcp, fetched fresh in CI, not in this repo at all.)
*.tfvars.json
*.auto.tfvars
*.json.key
*.pem
